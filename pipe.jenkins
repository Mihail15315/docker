
pipeline {
    agent { 
        label 'master'
        }
    options {
        timestamps()
    }
    triggers { pollSCM('* * * * *') }
    stages {
        stage("First step") {
            steps {
                dir('dock'){
                    sh 'docker build -t toolboc:latest . '
                }
                sh 'hostname'
            }
        }
    }
}
