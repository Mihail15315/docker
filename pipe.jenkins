
pipeline {
    agent { 
        label 'master'
        }
    options {
        timestamps()
    }
    triggers { pollSCM('* * * * *') }
    stages {
        stage("First step") {
            steps {
                dir('dock'){
                    sh 'docker build -t misha1607/toolboc:latest . '
                }
                sh 'hostname'
            }
        }
    }
}
